/*
[2023-09-16 29751 삼각형]
*/
#include <iostream>
#include <cmath>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    float w, h, n;
    cin >> w >> h;
    n = w*h/2;
    
    cout << fixed;
    cout.precision(1);
    cout << n;
}